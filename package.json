{
  "name": "@elumixor/extensions",
  "version": "2.1.4",
  "description": "JavaScript/TypeScript extensions for Array, Set, and String prototypes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elumixor/extensions.git"
  },
  "files": [
    "./dist"
  ],
  "scripts": {
    "build": "bun run build:bundle && bun run build:declaration",
    "build:bundle": "bun build ./src/index.ts --outdir ./dist",
    "build:declaration": "tsc --project tsconfig.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun test",
    "release:patch": "npm version patch -m \"Release v%s\" && git push --tags",
    "release:minor": "npm version minor -m \"Release v%s\" && git push --tags",
    "release:major": "npm version major -m \"Release v%s\" && git push --tags"
  },
  "keywords": [
    "extensions",
    "array",
    "set",
    "string",
    "prototype",
    "utilities"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/bun": "^1.3.1",
    "typescript": "^5.7.2"
  }
}
